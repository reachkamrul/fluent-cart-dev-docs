{
	"info": {
		"_postman_id": "app-api-collection-001",
		"name": "App",
		"schema": "https://schema.getpostman.com/json/collection/v2.0.0/collection.json",
		"_exporter_id": "23389172",
		"description": "FluentCart App API collection for application initialization and attachment management"
	},
	"item": [
		{
			"name": "Initialize App",
			"request": {
				"auth": {
					"type": "basic",
					"basic": {
						"password": "{{password}}",
						"username": "{{username}}"
					}
				},
				"method": "GET",
				"header": [],
				"url": {
					"raw": "{{base_url}}/wp-json/fluent-cart/v2/app/init",
					"host": ["{{base_url}}"],
					"path": ["wp-json", "fluent-cart", "v2", "app", "init"]
				},
				"description": "Retrieve application initialization data including REST API info, asset URLs, translations, and shop configuration."
			},
			"response": []
		},
		{
			"name": "Get Attachments",
			"request": {
				"auth": {
					"type": "basic",
					"basic": {
						"password": "{{password}}",
						"username": "{{username}}"
					}
				},
				"method": "GET",
				"header": [],
				"url": {
					"raw": "{{base_url}}/wp-json/fluent-cart/v2/app/attachments",
					"host": ["{{base_url}}"],
					"path": ["wp-json", "fluent-cart", "v2", "app", "attachments"]
				},
				"description": "Retrieve all image attachments from the WordPress media library."
			},
			"response": []
		},
		{
			"name": "Upload Attachments",
			"request": {
				"auth": {
					"type": "basic",
					"basic": {
						"password": "{{password}}",
						"username": "{{username}}"
					}
				},
				"method": "POST",
				"header": [],
				"body": {
					"mode": "formdata",
					"formdata": [
						{
							"key": "file",
							"type": "file",
							"src": []
						}
					]
				},
				"url": {
					"raw": "{{base_url}}/wp-json/fluent-cart/v2/app/upload-attachments",
					"host": ["{{base_url}}"],
					"path": ["wp-json", "fluent-cart", "v2", "app", "upload-attachments"]
				},
				"description": "Upload an image file to the WordPress media library."
			},
			"response": []
		}
	],
	"variable": []
}

